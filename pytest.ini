[pytest]
# 'app' paketini repo kokunden import edebilmek icin. Bu satir olmadan testler
# yalnizca `python -m pytest` ile calisir (o cagri CWD'yi sys.path'e ekler);
# duz `pytest` cagrisi -- CI'da kullanilan bicim -- ModuleNotFoundError verir.
pythonpath = .
testpaths = tests
