.env
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Browser profile and runtime outputs
data/
profile/
artifacts/
*.log
*.png
!docs/*.png

# Maintainer-only DOM canary. Runs as its own container against my deployment
# and is wired to my gist and this repo's issues, so it is nothing a user can
# run. Never was part of the published image either -- the Dockerfile only
# copies app/ and scripts/.
canary/
